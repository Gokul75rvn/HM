const AllEntitiesDirectory = () => {
  return (
    <main className="p-6">
      <h1 className="text-2xl font-semibold mb-4">All Entities Directory</h1>
      <p className="text-gray-600">
        This placeholder lists every configured dashboard or entity in the system. Replace it with
        the actual directory content when the data source is ready.
      </p>
    </main>
  );
};

export default AllEntitiesDirectory;
